#include "hufman.h"

char* ks_zip_hufman_compress(const char* data)
{
    ks_unused(data);
    return 0;
}

char* ks_zip_hufman_uncompress(const char* data)
{
    ks_unused(data);
    return 0;
}